# 02 - Cálculo de Retângulo

Programa para ler a largura e altura de um retângulo e exibir sua área, perímetro e diagonal.

### Conceitos aplicados:
* Orientação a Objetos (atributos e métodos)
* Uso da biblioteca `Math` (`Math.sqrt` e `Math.pow`)
* Formatação de saída com `String.format()`
